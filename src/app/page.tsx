"use client";

import MailPage from "@/components/mail/mail-page";

export default function Home() {
  return (
    <div className="relative flex min-h-screen flex-col bg-background font-[family-name:var(--font-geist-sans)]">
      <MailPage />
    </div>
  );
}
